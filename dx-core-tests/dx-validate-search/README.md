# dx-validate-search pipeline

Validate search index is working in a given server using rest api and dxclient.

Imports following artifacts:
1. Imports WCM TiKaLibrary
2. Start crawlers
3. Validate we can search by content of the file
